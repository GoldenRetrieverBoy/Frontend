# Блок 10. Тестирование

[План](../README.md) · [Назад](../fundamentals/algorithms.md) · [Далее](../engineering/tooling-git.md)

<a id="test-01"></a>

## TEST-01. Какие тесты выбрать и что такое тестируемый код?

**Ответ.** Выбираю уровень по риску: unit проверяет локальную логику, integration — взаимодействие частей, component — поведение компонента в заданном окружении, end-to-end — пользовательский путь через систему. Границы названий в командах различаются, поэтому уточняю, что реально включено в тест.

**Пример.** Формула скидки — быстрый unit; форма с валидацией и отправкой — component/integration; успешное оформление заказа — небольшой набор E2E. Тестируемый код имеет явные входы/выходы, контролируемые время и зависимости, отделённые эффекты.

**Практика:** распределить риски оплаты между уровнями и назвать то, что нельзя доказать браузерным тестом с полностью замоканным backend.

**Критерий самопроверки:** Для каждого риска выбран тест, который действительно включает проверяемую границу.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Расчёт суммы и округление проверяем unit-тестами по контракту. Ввод и блокировку повторной отправки — компонентным тестом. Авторизацию, идемпотентность и состояние заказа — интеграцией с реальным серверным обработчиком и хранилищем в тестовой среде. Небольшой E2E проходит платёжный сценарий через согласованную тестовую интеграцию. Полностью замоканный backend не доказывает отсутствие двойного списания или корректность серверных прав.

</details>

<a id="test-02"></a>

## TEST-02. Что такое mock, stub, spy и fixture?

**Ответ.** Fixture готовит входы и окружение; stub возвращает заданный ответ; spy фиксирует вызовы; mock часто объединяет управляемую замену и проверку взаимодействия. Важнее согласованный контракт теста, чем спор о названии.

```ts
import { expect, it, vi } from 'vitest';
it('уведомляет после сохранения', async () => {
  const save = vi.fn().mockResolvedValue({ id: '1' });
  const notify = vi.fn();
  const order = await save();
  notify(order.id);
  expect(notify).toHaveBeenCalledWith('1');
});
```

Это демонстрация API spy, не полноценный тест бизнес-функции: в настоящем тесте вызывается production-сценарий, а не его копия внутри it. Не следует мокать каждую внутреннюю функцию: тест начинает подтверждать собственные ожидания вместо интеграции.

**Практика:** выбери одну границу замены для формы: HTTP или composable, и объясни, какие ошибки второй подход может скрыть.

**Критерий самопроверки:** Названы реальные части тестируемой цепочки и ошибки, исключённые выбранной заменой.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Если заменяем HTTP-границу, компонент и composable исполняются вместе: видны ошибки URL, состояний загрузки и обработки ответа в пределах эмуляции. Если заменяем composable, проверяем преимущественно отображение и события компонента, но скрываем гонки, очистку и разбор HTTP внутри composable. Для формы с риском асинхронной ошибки выбираем первую границу; реальные свойства сети остаются отдельной проверкой.

</details>

<a id="test-03"></a>

## TEST-03. Что показывает coverage?

**Ответ.** Покрытие показывает, какие строки/ветви/функции исполнялись, а не насколько верны проверки. 100% строк не доказывают правильность результата, полноту требований или проверку границ. Использую coverage как сигнал непройденных ветвей вместе с анализом рисков.

```ts
import { expect, it } from 'vitest';
const canCancel = (status: string) => status === 'draft';
it.each([
  ['draft', true], ['paid', false], ['cancelled', false],
])('отмена для %s', (status, expected) => {
  expect(canCancel(status)).toBe(expected);
});
```

Параметризация убирает повтор тестового сценария, сохраняя понятные случаи. **Практика:** для ограничения «до 10 включительно» проверь 9, 10, 11 и недопустимые входы согласно контракту. **Ошибка:** единственная проверка «не бросает исключение» для расчётной функции.

**Критерий самопроверки:** Есть утверждения о допустимости каждой границы, а не только о выполнении строк.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Фиксируем контракт: допустимы целые числа от 0 до 10 включительно. Тогда 9 и 10 допустимы, 11 — нет; дополнительно 0 допустим, -1, NaN, Infinity, 1.5 и строка "10" отклоняются. Для другого предметного контракта меняем ожидаемые результаты явно. Проверка результата на 10 обнаружит ошибочную замену <= на < даже при прежнем покрытии строк.

</details>

<a id="test-04"></a>

## TEST-04. Как тестировать async и таймеры без flaky-тестов?

**Ответ.** Ожидаю наблюдаемое завершение операции, а не случайный sleep. Управляю таймерами и Promise на границах. После теста восстанавливаю окружение; очищаю моки, подписки и компоненты, чтобы тесты не зависели друг от друга.

```ts
import { expect, it, vi } from 'vitest';
it('вызывает обработчик по таймеру', () => {
  vi.useFakeTimers();
  try {
    const fn = vi.fn();
    setTimeout(fn, 100);
    vi.advanceTimersByTime(99);
    expect(fn).not.toHaveBeenCalled();
    vi.advanceTimersByTime(1);
    expect(fn).toHaveBeenCalledTimes(1);
  } finally { vi.useRealTimers(); }
});
```

В смешанном async-коде могут понадобиться асинхронные методы продвижения таймеров и ожидание Promise. Vue nextTick ждёт обновление Vue, а не произвольный сетевой Promise.

**Практика:** управляемыми Promise заверши второй поиск раньше первого и докажи, что первый не перезаписывает данные и loading. **Ошибка:** чинить flaky-тест увеличением sleep без поиска причины.

**Критерий самопроверки:** Старая операция не меняет данные, ошибку и индикатор новой при обоих порядках завершения.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Создаём два deferred Promise и запускаем поиск A, затем B. Завершаем B успешно, убеждаемся в данных B и loading=false; завершаем A с успехом, затем в отдельном тесте с ошибкой — состояние B сохраняется. Ещё один случай: A заканчивается, пока B ожидает, loading остаётся true. Продвигаем именно очередь Promise/обновлений используемой среды, а не ждём произвольный sleep.

</details>

<a id="test-05"></a>

## TEST-05. Что проверять в компоненте?

**Ответ.** Контракт пользователя и родителя: видимый текст, состояния, доступные действия, события и payload. Выбор реализации store или название внутреннего метода обычно не должны ломать тест поведения.

**Пример.** Вводим email, отправляем форму, видим loading, затем успех. Отдельно проверяем ошибку, повторную отправку, клавиатуру и отсутствие события при невалидных данных. DOM-эмулятор не воспроизводит реальную геометрию и всё браузерное поведение; layout и фокус сложного диалога проверяем в браузере.

**Практика:** составь 5 проверок формы заказа и объясни, почему огромный snapshot всей разметки слабее целевых утверждений. Для селекторов сначала доступны роли/имена; устойчивый test id полезен там, где семантика не различает элементы.

**Критерий самопроверки:** Пять проверок наблюдают пользовательский результат и полезно падают при его нарушении.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Проверки: неверное поле показывает связанную ошибку и не отправляет запрос; корректная форма отправляет нужный payload; во время ожидания повтор не отправляется; ошибка API сохраняет введённое и позволяет повторить; успех показывает подтверждение заказа. Селекторы используют роли/имена, где они различают элементы. Общий snapshot может заметить смену class, но пропустить отсутствие важной проверки запроса или неправильный эффект.

</details>

<a id="test-06"></a>

## TEST-06. Как проводить нагрузочное тестирование?

**Ответ.** Сначала определяю цель и модель нагрузки: сценарии, интенсивность, конкурентность, данные, длительность, прогрев и критерии остановки. Смотрю throughput, ошибки, p95/p99, ресурсы и насыщение. Нагрузка на HTTP API и измерение отзывчивости браузера — разные проверки.

**Пример.** При росте запросов с 20 до 100 в секунду p95 растёт, CPU низкий, пул соединений исчерпан. Увеличение CPU не обязательно поможет. Проверяем зависимость и очередь, не делаем вывод только по среднему времени.

**Практика:** опиши тест SSR-каталога в изолированной среде: ожидаемый профиль трафика, лимиты, серверные метрики, ошибки и параллельный browser trace. Добавь сравнение до/после в одинаковых условиях.

**Критерий самопроверки:** План задаёт ступени нагрузки, длительность, наблюдаемые метрики и условия остановки; сравнение повторяется на одинаковых данных.

<details>
<summary>Разбор плана нагрузки — открой после самостоятельной попытки</summary>

В изолированной среде фиксируем набор страниц и данных, версию сборки и состояние кэша. Учебный профиль: 5 минут прогрева, затем по 10 минут на согласованных ступенях 20, 50 и 100 запросов/с; это пример, реальные пределы выбирает владелец среды. До запуска задаём допустимую долю ошибок, задержку и лимиты ресурсов с остановкой при превышении. Собираем throughput, p95/p99, ошибки, CPU, память, пул соединений и время зависимостей. Параллельно записываем один и тот же браузерный сценарий: HTTP-нагрузка не измеряет отзывчивость UI. После одного изменения повторяем профиль с теми же условиями и проверяем, что генератор нагрузки сам не стал ограничением.

</details>

### Практика с числами

При одинаковом сценарии в тестовой среде 20 запросов/с дают p95=180 мс и 0% ошибок. При 100 запросах/с p95=2400 мс, 3% ошибок, CPU=25%, а очередь ожидания подключения к БД растёт. Можно ли сразу увеличить число CPU?

<details>
<summary>Разбор</summary>

Наблюдения указывают на ожидание соединений или зависимость, но ещё не доказывают причину. Сопоставь метрики пула, время запросов к БД и размер очереди. Проверь длительные запросы и удержание соединений. Измени одну подтверждённую причину и повтори тот же профиль нагрузки. Сравни throughput, ошибки, p95/p99 и ресурсы. Среднее время и один низкий CPU не подтверждают отсутствие узкого места.

</details>

## Проверка и источники

Написать тест гонки, разобрать flaky timer, выбрать уровни тестирования и план нагрузки. Источники: [Vitest timers](https://vitest.dev/guide/mocking/timers), [Vue testing](https://vuejs.org/guide/scaling-up/testing.html), [k6 documentation](https://grafana.com/docs/k6/latest/).

### Дополнительные вопросы для интервью

- Чем функциональный тест отличается от нагрузочного?
- Как определить модель нагрузки: сценарий, интенсивность, конкурентность, длительность, прогрев и тестовые данные?
- Что показывают throughput, error rate, p50, p95 и p99?
- Почему среднее время ответа может скрывать проблему?
- Как найти узкое место, сопоставив результаты нагрузки с CPU, памятью, соединениями, очередями и зависимостями?
- Как провести нагрузочный тест безопасно и сравнимо?
- Чем нагрузочный тест API отличается от проверки производительности браузерного интерфейса?
