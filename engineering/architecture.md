# Блок 08. Проектирование и архитектура

[План](../README.md) · [Назад](../platform/security.md) · [Далее](../fundamentals/algorithms.md)

<a id="arc-01"></a>

## ARC-01. Инкапсуляция, полиморфизм, наследование и композиция — зачем?

**Ответ.** Инкапсуляция защищает инварианты и скрывает детали реализации; полиморфизм позволяет работать с разными реализациями через общий контракт; наследование связывает типы и поведение. Композиция собирает объект из сотрудничающих частей и часто уменьшает жёсткую связь с базовым классом. Агрегация описывает связь с частью, которая может существовать независимо; это модель владения, а не специальный оператор JS.

```ts
interface Notifier { send(message: string): Promise<void> }
class Checkout {
  constructor(private notifier: Notifier) {}
  async notify() { await this.notifier.send('Заказ оформлен'); }
}
```

Checkout зависит от контракта: можно передать email-реализацию или тестовую. TypeScript interface исчезает в JS. Абстрактный класс может иметь общую runtime-реализацию; interface описывает форму без общей реализации. Статический метод принадлежит классу, обычный — поведению экземпляров.

**Практика:** замени иерархию «заказ с email, заказ с SMS, заказ с обоими» композицией. **Ошибка:** называть любую функцию с private полем хорошей инкапсуляцией, если инварианты всё равно нарушаются извне.

**Критерий самопроверки:** Email и SMS можно включить вместе без нового подкласса заказа.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Заказ хранит данные и инварианты, а отправка получает набор функций уведомления: например notifyOrder(order, [sendEmail, sendSms]). Для email передаём одну функцию, для обоих каналов — две. Проверяем пустой набор и отказ одного канала; политику продолжения задаём отдельно. Не используем наследование ради перебора комбинаций независимых возможностей.

</details>

<a id="arc-02"></a>

## ARC-02. Как применять SOLID без лишних абстракций?

**Ответ.** SRP — согласованные причины изменения; OCP — расширение подходящей точки без переписывания устойчивой части; LSP — заменяемость с соблюдением контракта; ISP — узкие интерфейсы для потребителей; DIP — политика зависит от абстракций, а не деталей. Это критерии проектирования, а не требование создать класс для каждого действия.

**Пример.** Расчёт цены принимает функцию скидки. Новая стратегия меняет скидку, но не оркестрацию. Если реализация «скидки» внезапно отправляет платёж, контракт нарушен. DRY касается дублирования знания; две случайно похожие формы не обязательно надо объединять. KISS уменьшает ненужную сложность, YAGNI откладывает неподтверждённые возможности.

**Практика:** общая форма с 20 boolean props обслуживает три продукта. Назови признаки неудачной абстракции, предложи разделение и оцени миграцию. **Уточнение:** когда дублирование дешевле общей библиотеки? При разных направлениях изменений и неустойчивом общем контракте.

**Критерий самопроверки:** Выделены действительно разные сценарии; миграция сохраняет проверяемое поведение каждого продукта.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Сначала собираем используемые сочетания 20 флагов и противоречивые состояния. Разделяем продуктовые сценарии на отдельные формы, оставляем общими только устойчивые поля и правила. Мигрируем одного потребителя с проверками, затем остальных; устаревший API удаляем после перехода всех. Если направления изменений различаются, временное дублирование может быть дешевле нового универсального конфигуратора.

</details>

<a id="arc-03"></a>

## ARC-03. Какие паттерны реально применяются на Frontend?

**Ответ.** Выбираю паттерн по проблеме и цене. Factory централизует создание; Builder — последовательную сборку сложного объекта; Singleton — единственный экземпляр в выбранной области, но усложняет изоляцию/SSR. Strategy заменяет алгоритм, Adapter согласует интерфейс, Facade упрощает подсистему, Decorator добавляет поведение, Observer уведомляет подписчиков, Composite работает с деревом единообразно.

```ts
type Discount = (price: number) => number;
const withoutDiscount: Discount = price => price;
const tenPercent: Discount = price => price * 0.9;
const quote = (price: number, discount: Discount) => discount(price);
console.log(quote(100, tenPercent)); // 90
```

Это Strategy без классов. Учебная арифметика не задаёт production-правила округления денег. Observer требует unsubscribe; Singleton со сессией на сервере может смешать пользователей. Builder не нужен объекту из двух очевидных полей.

**Практика:** для двух поставщиков с разными DTO предложи Adapter к доменной модели, затем Facade для сценария поиска. Объясни, почему это две разные роли.

**Критерий самопроверки:** Разница DTO скрыта адаптерами, порядок работы нескольких частей — фасадом.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Поставщик A отдаёт {code,title}, B — {id,label}; два Adapter приводят их к {id,name}. Facade search(query) выбирает или опрашивает поставщиков, вызывает адаптеры и применяет согласованную политику ошибок/дублей. Проверяем обе формы DTO и отказ одного поставщика. Adapter преобразует контракт, Facade упрощает доступ к сценарию; одна функция может совмещать роли, но их причины изменения различаются.

</details>

<a id="arc-04"></a>

## ARC-04. Что разобрать «за пределами каталога паттернов»?

**Ответ.** Матрица не задаёт закрытый список. Берём state machine для явных переходов, circuit breaker для временного ограничения обращений к отказывающей зависимости, bulkhead для изоляции ресурсов и stale-while-revalidate для сочетания скорости/актуальности. Нужно объяснить конкретный контекст применения, а не заявлять, что эти названия отсутствуют на каком-то сайте.

**Пример state machine.** `idle → submitting → success | error`; повторная отправка в submitting запрещена, retry разрешён из error. Это предотвращает невозможные комбинации нескольких boolean-полей. Однако серверная идемпотентность всё равно нужна.

Антипаттерны: God component, глобальная шина без владельца/очистки, общий кэш без границы пользователя, бесконечный retry, абстракция с множеством исключений, распределённый монолит с синхронно сцепленными релизами. Их оцениваем по последствиям, не по эстетике.

**Практика:** API упал, каждый клиент делает немедленный retry. Объясни усиление сбоя, backoff с jitter, лимит попыток и ограничения повторов неидемпотентных операций.

### Дополнительные вопросы для интервью

- Что такое антипаттерн? Чем он отличается от просто неидеального кода?
- Какие последствия создают God Component, глобальное изменяемое состояние и общий модуль `utils`, не имеющий чёткой области ответственности?
- Какие признаки показывают, что абстракция стала сложнее дублирования, которое должна была устранить?
- Как обнаруживать архитектурные проблемы во время code review и предлагать постепенное исправление?
- Как state machine помогает исключить невозможные состояния интерфейса?
- Что такое Circuit Breaker и почему его обычно реализуют на серверной или инфраструктурной границе, а не только в браузере?
- Что такое Bulkhead и как изоляция ресурсов уменьшает влияние отказа?
- Как работает stale-while-revalidate и какой компромисс между скоростью и актуальностью он создаёт?
- Что такое retry storm и как помогают exponential backoff, jitter и ограничение попыток?
- Какие архитектурные антипаттерны приводят к связанным релизам, общей точке отказа и невозможности изолировать данные пользователей?

**Критерий самопроверки:** Предложенный retry ограничивает усиление нагрузки и не дублирует неидемпотентный эффект.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Немедленные повторы множат трафик во время сбоя. Ограничиваем попытки и общий срок, увеличиваем задержку с jitter, соблюдаем серверный сигнал ожидания. Не повторяем окончательные ошибки входных данных. Создание требует ключа идемпотентности или проверки исхода; отмена прекращает ожидание клиента. Проверяем длительный отказ, восстановление и одновременный запуск множества клиентов в модели нагрузки.

</details>

<a id="arc-05"></a>

## ARC-05. Как объяснить FSD и границы модулей?

**Ответ.** В FSD слои разделяют ответственность, slices группируют связанную предметную функциональность, segments — назначение кода внутри. Основные слои: app, pages, widgets, features, entities, shared. Не обязательно создавать все. Зависимости между slices в общем случае направлены вниз; публичный API ограничивает знание внутренних файлов. App/Shared имеют особую структуру; для связей entities есть явные cross-reference API.

**Пример.** Страница заказа собирает отображение сущности Order и действие отмены. Общая кнопка не знает о заказе. Единственный экранный блок без переиспользования можно оставить в странице: не всякая кнопка становится feature. Processes в текущей документации устарел.

**Практика:** feature A импортирует внутренний store feature B. Предложи поднять координацию выше, выделить общий смысл или пересмотреть неверно проведённую границу. Перемещение всего в shared не решает связанность.

**Критерий самопроверки:** Устранён импорт внутреннего store между features; shared не начинает знать о предметных действиях.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Если A и B независимы, координацию переносим в страницу/widget, передаём данные и callbacks через публичные API. Если они образуют один сценарий, пересматриваем разделение. Если общий смысл — сущность, выделяем её контракт ниже, не копируя туда всю логику. Проверяем граф импортов и сценарий, использующий обе части; замена внутреннего пути на public index другой feature сама по себе не исправляет зависимость одного слоя.

</details>

<a id="arc-06"></a>

## ARC-06. CSR, SSR, SSG, SPA и MPA — как выбирать?

**Ответ.** CSR строит интерфейс на клиенте; SSR генерирует HTML на сервере при запросе; SSG заранее генерирует HTML. SPA/MPA описывают характер навигации и документов, это другая ось: SPA может использовать SSR для первой загрузки. Hydration связывает серверную разметку с клиентским поведением и требует согласованного начального вывода.

| Сценарий | Возможный выбор | Цена |
|---|---|---|
| Закрытый кабинет | CSR | Зависимость первого UI от JS и запросов |
| Публичный меняющийся каталог | SSR с продуманным кэшированием | Сервер, нагрузка, hydration |
| Редко меняющиеся страницы | SSG | Время сборки и актуализация |

**Пример ошибки:** сервер выводит локальное время или случайное значение, клиент другое. Исправляем детерминированность и передачу начальных данных, а не отключаем предупреждения. **Практика:** выбери подход для каталога и кабинета одного продукта, учитывая SEO, персонализацию и стоимость.

### Дополнительные вопросы для интервью

- Чем отличаются CSR, SSR и SSG? На каком этапе и где формируется HTML в каждом подходе?
- Какие преимущества и недостатки CSR, SSR и SSG по SEO, времени ответа, нагрузке, персонализации и актуальности данных?
- Что такое hydration и почему возникает hydration mismatch?
- Чем SPA отличается от MPA?
- Почему SPA может использовать SSR, хотя названия иногда ошибочно считают противоположностями?
- Как выбрать rendering-подход для публичного каталога и закрытого личного кабинета?
- Что означают cloud, CDN и S3-подобное хранилище в архитектуре Frontend-приложения?

**Критерий самопроверки:** Выбор привязан к SEO, персонализации, актуальности и стоимости эксплуатации.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Для публичного часто меняющегося каталога рассматриваем SSR с контролем кэширования, для редко меняющегося — SSG с приемлемой задержкой обновления. Закрытый кабинет может использовать CSR, если устраивает первая загрузка. Проверяем персональные данные в кэше, начальный HTML, гидратацию и деградацию при отказе API. SPA/MPA выбираем отдельно как характер навигации; одного требования SEO недостаточно для всей архитектуры.

</details>

<a id="arc-07"></a>

## ARC-07. Cloud, S3, CDN, BFF и API — как связаны?

**Ответ.** Объектное хранилище держит объекты, CDN доставляет/кэширует, cloud предоставляет инфраструктурные сервисы. BFF адаптирует серверные контракты под конкретный клиент: агрегирует, нормализует и контролирует серверные credentials. Это дополнительный сервис с задержками, отказами и стоимостью поддержки.

**Пример.** Браузер загружает разрешённый файл по ограниченной подписанной ссылке, backend проверяет права и задаёт условия. Ссылка не заменяет валидацию содержимого. Для API нескольких клиентов изменения должны быть совместимыми: добавляем новое поле, мигрируем потребителей, только потом удаляем старое с согласованной политикой.

**Практика:** три сервиса формируют карточку заказа, один недоступен. Определи обязательные и необязательные данные, таймауты, частичный ответ и наблюдаемость. REST раскрывается здесь как прикладное дополнение к HTTP; отдельная строка REST в источнике находится в TODO.

**Критерий самопроверки:** Недоступная необязательная часть не скрывает обязательную, а отсутствие обязательной не маскируется успехом.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Для примера цена и доступность обязательны, рекомендации необязательны. Ставим бюджеты времени на зависимости, возвращаем рекомендации как недоступные при их отказе; при отсутствии цены блокируем оформление и показываем явную ошибку. Фиксируем, какой сервис не ответил, без утечки credentials. Проверяем каждый отказ по отдельности, совместный отказ и медленный ответ; пустой массив не должен скрывать неизвестный исход.

</details>

<a id="arc-08"></a>

## ARC-08. Как безопасно рефакторить и выделять библиотеку?

**Ответ.** Сначала фиксирую ожидаемое поведение и границы изменения, затем меняю небольшими шагами с проверкой значимых сценариев. Для библиотеки нужны несколько реальных потребителей, устойчивый публичный контракт, версия, миграция и владелец. Общий код без поддержки может дороже обойтись всем продуктам.

**Пример.** Перенос HTTP-клиента меняет трактовку 204: прежний код не читал JSON, новый пытается. Проверяем контракт пустого ответа, отмену, credentials, ошибки и retry. Снижение количества строк не доказывает сохранение поведения.

**Практика:** предложи план замены критичного модуля с постепенным включением, метриками, обратным переключением и условием удаления старого кода. ADR: контекст → варианты → решение → последствия.

**Критерий самопроверки:** Есть этапы, наблюдаемый критерий успеха, обратное переключение и условие удаления старого модуля.

<details>
<summary>Разбор практики — открой после самостоятельной попытки</summary>

Фиксируем контракт и регрессионные сценарии, добавляем новую реализацию за переключателем, включаем на ограниченном сегменте. Сравниваем ошибки и результаты по согласованным метрикам; для записи не запускаем оба эффекта ради сравнения. При превышении порога возвращаем старую реализацию. После периода наблюдения и миграции потребителей удаляем старый путь и переключатель, обновляем ADR.

</details>

## Проверка и источники

За 45 минут спроектировать поиск и оформление заказа: ограничения, данные, границы, отказы, доставка, проверка. Источники: [FSD layers](https://feature-sliced.design/docs/reference/layers), [Vue SSR](https://vuejs.org/guide/scaling-up/ssr.html). GRASP обязателен по матрице для Senior и разобран в ARC-09. Clean Architecture и DDD находятся в TODO исходника; не считаем их утверждёнными требованиями.

<a id="arc-09"></a>

## ARC-09. Как GRASP помогает распределять ответственность?

**Короткий ответ.** GRASP — принципы назначения ответственности объектам: Information Expert, Creator, Controller, Low Coupling, High Cohesion, Polymorphism, Pure Fabrication, Indirection и Protected Variations. Начинаю с того, кто владеет необходимыми данными, и проверяю связность и количество причин изменения.

**Пример по шагам.** В корзине есть позиции и цены. Cart рассчитывает сумму как Information Expert. CheckoutController координирует оформление, но не знает формат HTTP. OrderRepository изолирует сохранение: это Pure Fabrication и Indirection. Контракт платёжного шлюза защищает оформление от смены провайдера — Protected Variations.

**Практика.** Компонент оформления одновременно считает сумму, вызывает SDK оплаты и сохраняет заказ. Распредели эти обязанности и объясни два принципа GRASP.

<details>
<summary>Разбор практики</summary>

Расчёт отдаём модели корзины или чистой доменной функции, SDK прячем за адаптером, сценарий оформления координирует вызовы. Компонент управляет вводом и отображением. Low Coupling уменьшает знание SDK, High Cohesion объединяет связанные обязанности. Не нужны отдельные классы для каждой строки: функции с явными параметрами также подходят.

</details>

**Уточнение.** Чем Information Expert отличается от помещения всей логики в компонент, у которого случайно есть все данные? Важны предметное владение данными и согласованная ответственность, а не техническая доступность переменных.

<a id="arc-10"></a>

## ARC-10. Чем императивный, декларативный и функциональный подходы отличаются на одном примере?

**Короткий ответ.** Императивный код задаёт последовательность действий; декларативный описывает желаемый результат. Функциональный подход строит вычисления через функции, композицию и контроль эффектов. Это пересекающиеся характеристики, а не три взаимоисключающих языка.

**Пример по шагам.** Для [1, 2, 3] цикл с push выбирает нечётные числа пошагово. Выражение values.filter(x => x % 2 !== 0) описывает отбор. Если функция не меняет вход и зависит только от него, такой отбор является чистым вычислением.

**Практика.** Перепиши цикл отбора доступных товаров через filter и сравни мутации входного массива и локального результата.

<details>
<summary>Разбор практики</summary>

filter создаёт новый массив, но объекты внутри остаются теми же ссылками. Локальный push в свежий массив сам по себе не делает функцию нечистой: важны наблюдаемые эффекты за её границами. Изменять свойства входного товара — уже изменение внешних данных.

</details>

**Уточнение.** Почему map с записью в глобальный store не становится чистым только из-за использования map?
