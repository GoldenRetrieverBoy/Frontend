# Вопросы, которые появляются на Middle 2–3

[Вернуться к общему плану](../README.md)

Этот файл — сокращённый маршрут подготовки. Он включает только требования, которые впервые получают `+` в столбце Middle 2 или Middle 3 относительно предыдущего уровня на листе `TechSkills Frontend`.

Важно: на собеседовании могут проверять и базу предыдущих уровней. Этот список помогает сначала сосредоточиться на разнице между Middle 1, Middle 2 и Middle 3.

## Middle 2: технические вопросы

### Docker и доставка

1. Что такое multi-stage build и почему он уменьшает production-образ?

#### Разбор вопроса 1

Для сборки Frontend-приложения обычно нужны:

- Node.js и npm;
- исходный код;
- зависимости из `node_modules`;
- TypeScript, Vite и другие инструменты сборки.

После `npm run build` браузеру нужны только готовые файлы из `dist`, например:

```text
dist/
├── index.html
└── assets/
    ├── app.js
    └── app.css
```

Хранить Node.js, исходники, инструменты сборки и весь `node_modules` в production-образе статического Frontend-приложения обычно не требуется.

Multi-stage build разделяет Dockerfile на несколько этапов:

```dockerfile
# Этап 1: сборка приложения
FROM node:22-alpine AS build

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build

# Этап 2: production-образ
FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
```

На первом этапе устанавливаются зависимости и собирается приложение:

```text
исходный код → Vite → dist
```

Второй `FROM` начинает новый этап с новым базовым образом. Команда:

```dockerfile
COPY --from=build /app/dist /usr/share/nginx/html
```

копирует из этапа `build` только готовую директорию `dist`. Поэтому финальный образ содержит Nginx и готовую статику, но не содержит Node.js, исходный код и `node_modules` из первого этапа.

```text
Этап build                         Финальный образ
├── Node.js                        ├── Nginx
├── node_modules                   └── dist
├── исходный код                         ↑
└── dist ────────────────────────────────┘
```

Это обычно даёт следующие преимущества:

- меньший размер production-образа;
- меньше данных для скачивания при деплое;
- меньше ненужных пакетов в runtime;
- меньшая поверхность атаки;
- понятное разделение сборки и запуска.

Первый этап может сохраняться в локальном build cache и использоваться при последующих сборках. Однако его содержимое не входит в финальный образ, если из него скопировали только `dist`.

**Короткий ответ для собеседования:**

> Multi-stage build разделяет сборку и запуск приложения на несколько этапов. На первом этапе Frontend собирается с помощью Node.js, а в финальный образ копируется только готовая статика. Поэтому production-образ не содержит исходники, `node_modules` и инструменты сборки, получается меньше и содержит меньше ненужных компонентов.

**Дополнительные вопросы интервьюера:**

- Почему второй `FROM` создаёт отдельный этап?
- Что именно делает `COPY --from=build`?
- Нужен ли Node.js в финальном образе статической SPA?
- Останется ли `node_modules` в финальном образе?
- Чем будет отличаться Dockerfile для SSR-приложения, которому Node.js нужен во время выполнения?

**Проверка понимания:**

Почему в финальном образе нет `node_modules`, хотя на первом этапе выполнялся `npm ci`?

2. Как построить Dockerfile для Frontend-приложения: установка зависимостей, сборка и раздача готовой статики?
3. Как порядок `COPY` и `RUN` влияет на Docker build cache и время повторной сборки?
4. Чем должны различаться production и staging: конфигурацией, артефактом, доменами, логированием и интеграциями?
5. Почему секрет нельзя помещать в переменную окружения, которая встраивается в клиентский bundle?

Материал: [OPS-04–05](../platform/delivery-node.md).

### Git, GitLab и интеграции

6. Чем GitFlow отличается от Trunk Based Development? Какие преимущества, риски и требования к команде есть у каждого подхода?
7. Как выбрать процесс ветвления для частых релизов Frontend-приложения?
8. Что такое webhook, как он доставляет события и почему получатель должен учитывать повторную доставку?
9. Как проверить подлинность webhook и не допустить повторного выполнения операции?
10. Какие настройки GitLab защищают репозиторий и pipeline: роли, protected branches, approval rules, secrets и ограничения запуска?

Материал: [TOOL-07](../engineering/tooling-git.md), CI/CD также [OPS-05](../platform/delivery-node.md).

### CDN и архитектура доставки

11. Что такое CDN и какой путь проходит запрос пользователя до статического ресурса?
12. Что такое cache hit, cache miss, TTL, invalidation и purge?
13. Как безопасно кэшировать HTML и файлы с content hash после релиза?
14. Почему после релиза часть пользователей может получить HTML новой версии и JavaScript старой версии или наоборот?
15. Как связаны объектное хранилище S3-типа, CDN и браузерный HTTP-кэш?

Материал: [NET-04](../platform/networking.md), [ARC-07](../engineering/architecture.md).

### Безопасность

16. Что такое Content Security Policy и от каких сценариев она помогает защищаться?
17. Чем `Content-Security-Policy-Report-Only` отличается от блокирующей политики?
18. Что такое nonce и hash в CSP? Почему `unsafe-inline` ослабляет политику?
19. Для чего нужны HSTS, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options` и директива CSP `frame-ancestors`?
20. Почему CORS относится к политике доступа браузера, а не к аутентификации пользователя?
21. Почему `X-XSS-Protection` считается устаревшим и не должен быть основной защитой от XSS?

Материал: [SEC-04](../platform/security.md). Полный контекст XSS и CORS: [SEC-01](../platform/security.md), [NET-05](../platform/networking.md).

### Паттерны и антипаттерны

22. Что такое антипаттерн? Чем он отличается от просто неидеального кода?
23. Какие последствия создают God Component, глобальное изменяемое состояние и общий модуль `utils`, не имеющий чёткой области ответственности?
24. Какие признаки показывают, что абстракция стала сложнее дублирования, которое должна была устранить?
25. Как обнаруживать архитектурные проблемы во время code review и предлагать постепенное исправление?

Материал: [ARC-02–04](../engineering/architecture.md).

### Rendering и устройство приложения

26. Чем отличаются CSR, SSR и SSG? На каком этапе и где формируется HTML в каждом подходе?
27. Какие преимущества и недостатки CSR, SSR и SSG по SEO, времени ответа, нагрузке, персонализации и актуальности данных?
28. Что такое hydration и почему возникает hydration mismatch?
29. Чем SPA отличается от MPA?
30. Почему SPA может использовать SSR, хотя названия иногда ошибочно считают противоположностями?
31. Как выбрать rendering-подход для публичного каталога и закрытого личного кабинета?
32. Что означают cloud, CDN и S3-подобное хранилище в архитектуре Frontend-приложения?

Материал: [ARC-06–07](../engineering/architecture.md).

### Service Worker

33. Чем Service Worker отличается от Web Worker?
34. Как проходят `install`, `waiting`, `activate` и получение контроля над страницей?
35. Как работают cache-first, network-first и stale-while-revalidate?
36. Как Service Worker может привести к загрузке устаревшей версии приложения?
37. Какие данные опасно кэшировать без учёта пользователя и авторизации?
38. Как спроектировать безопасное обновление Service Worker после релиза?

Материал: [WEB-05](../frontend/browser-web-api.md).

### Логи, профилирование и производительность

39. Как искать запросы и ошибки в ELK/Kibana: время, окружение, сервис, release, статус и correlation ID?
40. Чем лог отличается от метрики и трассировки? Когда нужен каждый сигнал?
41. Как с помощью Chrome DevTools Performance найти long task, тяжёлый JavaScript, layout и paint?
42. Как проверить гипотезу об узком месте и корректно сравнить результат до и после оптимизации?
43. Как работают gzip и Brotli? Какие ресурсы имеет смысл сжимать?
44. Как настроить сжатие в NGINX и проверить, что клиент действительно получил сжатый ответ?
45. Что измеряют LCP, INP и CLS? Какие типичные причины ухудшают каждую метрику?
46. Чем лабораторное измерение производительности отличается от данных реальных пользователей?
47. Почему хорошая оценка Lighthouse не гарантирует хорошие пользовательские метрики?

Материал: [PERF-01–05](../platform/performance-monitoring.md), доставка через NGINX: [OPS-06](../platform/delivery-node.md).

## Middle 3: технические вопросы

### Оптимизация Docker

1. Как уменьшить размер production-образа и доказать результат измерениями?
2. Как `.dockerignore`, multi-stage build, минимальный base image и набор runtime-файлов влияют на размер и безопасность?
3. Как ускорить холодную и повторную сборку Docker-образа?
4. Почему удаление файла в последующем слое не обязательно уменьшает размер предыдущих слоёв?
5. Как анализировать слои образа и находить причину резкого роста размера?

Материал: [OPS-04](../platform/delivery-node.md).

### Нагрузочное тестирование

6. Чем функциональный тест отличается от нагрузочного?
7. Как определить модель нагрузки: сценарий, интенсивность, конкурентность, длительность, прогрев и тестовые данные?
8. Что показывают throughput, error rate, p50, p95 и p99?
9. Почему среднее время ответа может скрывать проблему?
10. Как найти узкое место, сопоставив результаты нагрузки с CPU, памятью, соединениями, очередями и зависимостями?
11. Как провести нагрузочный тест безопасно и сравнимо?
12. Чем нагрузочный тест API отличается от проверки производительности браузерного интерфейса?

Материал: [TEST-06](../engineering/testing.md), [PERF-01](../platform/performance-monitoring.md).

### DNS

13. Что такое DNS и зачем он нужен?
14. Как проходит разрешение доменного имени с учётом браузерного, системного и resolver-кэша?
15. Чем отличаются записи A, AAAA, CNAME, TXT и NS?
16. Что такое TTL и почему изменение DNS не становится одновременно видимым всем пользователям?
17. Как диагностировать ситуацию: домен не открывается, но сервис по IP доступен?
18. Как DNS связан с CDN, балансировщиком и TLS-сертификатом?

Материал: [NET-01](../platform/networking.md).

### OWASP Top 10

19. Что такое OWASP Top 10 и почему это не полный чек-лист безопасности?
20. Какие категории входят в OWASP Top 10:2025 и какие из них особенно важны для Frontend-разработчика?
21. Как Broken Access Control проявляется в приложении с заказами?
22. Как ошибки конфигурации, цепочка поставки и слабое логирование приводят к инцидентам?
23. Чем insecure design отличается от отдельной ошибки реализации?
24. Как построить threat model для новой Frontend-функции: активы, границы доверия, угрозы и меры защиты?

Материал: [SEC-01–06](../platform/security.md).

### Паттерны и антипаттерны повышенной сложности

25. Как state machine помогает исключить невозможные состояния интерфейса?
26. Что такое Circuit Breaker и почему его обычно реализуют на серверной или инфраструктурной границе, а не только в браузере?
27. Что такое Bulkhead и как изоляция ресурсов уменьшает влияние отказа?
28. Как работает stale-while-revalidate и какой компромисс между скоростью и актуальностью он создаёт?
29. Что такое retry storm и как помогают exponential backoff, jitter и ограничение попыток?
30. Какие архитектурные антипаттерны приводят к связанным релизам, общей точке отказа и невозможности изолировать данные пользователей?

Материал: [ARC-04](../engineering/architecture.md).

### Kubernetes

31. Что такое Pod, Deployment, ReplicaSet и Service? Как они связаны?
32. Как проходит rolling update Frontend- или SSR-приложения?
33. Чем readiness probe отличается от liveness и startup probe?
34. Почему некорректная liveness probe способна усилить аварию?
35. Чем ConfigMap отличается от Secret? Почему base64 не является шифрованием?
36. Что означают CPU/memory requests и limits?
37. Как диагностировать `CrashLoopBackOff`, `ImagePullBackOff`, `OOMKilled` и неготовый Pod?
38. Как проверить, какая версия приложения действительно развёрнута и получает трафик?

Материал: [OPS-07](../platform/delivery-node.md).

### Graphite, Grafana и алерты

39. Чем Graphite отличается от Grafana?
40. Как метрика проходит от приложения до хранилища временных рядов и панели?
41. Какие метрики нужны для Frontend, SSR/BFF и процесса релиза?
42. Что такое cardinality и почему `userId` или `orderId` нельзя бездумно помещать в имя временного ряда или label?
43. Как построить Grafana-dashboard для диагностики оформления заказа?
44. Каким должен быть actionable alert: сигнал, окно, порог, минимальный объём, no-data, владелец и runbook?
45. Почему alert на каждую отдельную ошибку создаёт шум?
46. Почему нельзя просто усреднить p95 нескольких серверов и считать результат общим p95?

Материал: [PERF-06–07](../platform/performance-monitoring.md).

## Middle 2–3: практические и поведенческие вопросы

Эти вопросы основаны на `Responsibilities` и `SoftSkills`. Здесь нужен реальный пример по схеме: контекст → проблема → твоя роль → варианты → действия → результат → выводы.

### Ожидания Middle 2

1. Расскажи, как ты спроектировал или выделил общую библиотеку для нескольких продуктов.
2. Как ты рефакторил сложную или критичную часть системы без изменения поведения?
3. Как ты профилировал приложение и доказал эффект оптимизации?
4. Расскажи о риске, о котором ты заранее сообщил руководителю. Какие варианты предложил?
5. Как ты оцениваешь сложную задачу, учитывая неизвестность, зависимости и интеграцию?
6. Как ты связываешь техническое решение с целью бизнеса или OKR?
7. Как ты помог улучшить постановку задачи, процесс разработки или взаимодействие команды?
8. Как ты действовал при жёстком сроке или нескольких параллельных проблемах?

Материал: [MID-03, MID-05–06](../engineering/middle3-cases.md).

### Ожидания Middle 3

9. Расскажи об аварии: как оценил влияние, восстановил работу и предотвратил повторение?
10. Как ты организуешь postmortem и отличаешь первопричину от сопутствующих условий?
11. Как ты контролируешь технический долг и выбираешь, что исправлять первым?
12. Как определить, какая документация нужна команде, и обеспечить её актуальность?
13. Как ты измеряешь эффективность разработки, не превращая метрику в цель ради самой метрики?
14. Как ты координируешь работу нескольких разработчиков или команд?
15. Как составить план работ команды с зависимостями, контрольными точками и рисками?
16. Как действовать, если необходимо временно заменить тимлида?
17. Как снизить вероятность стрессовой ситуации и организовать коммуникацию во время инцидента?

Материал: [MID-01–06](../engineering/middle3-cases.md).

## Рекомендуемый порядок

1. Rendering, CDN и Service Worker.
2. Профилирование и Web Vitals.
3. Безопасность и OWASP.
4. Docker, CI/CD и Kubernetes.
5. DNS и диагностика сети.
6. Нагрузочное тестирование и мониторинг.
7. Паттерны, антипаттерны и инженерные кейсы.

Сначала проходим вопросы Middle 2, затем соответствующее углубление Middle 3. Например: multi-stage build → оптимизация образа; CSP → OWASP; профилирование → нагрузка и мониторинг.

## Как используем этот файл

На занятии берём один вопрос. Сначала ты отвечаешь своими словами. Затем разбираем механизм и пример, формулируем короткий ответ для собеседования и решаем один изменённый кейс. В [журнале](../roadmap/progress.md) отмечаем только самостоятельно подтверждённое знание.
